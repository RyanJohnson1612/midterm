<!DOCTYPE html>
<html lang="en">
  <!-- Head tag-->
  <%- include('../partials/_head', { title: `Order confirmation`}) %>
  <body>
    <!-- Navbar-->
    <%- include('partials/_nav') %>

    <!-- Order confirmation -->
    <div class="jumbotron text-center" style="background-color:transparent; color: black; height: 93vh; margin-bottom: 0;">
      <h1 class="display-3 color: white">Your Order</h1>
      <p class="lead">Order number: <%=id%></p>
      <p class="lead">Status: Order confirmed by restaurant </p>
      <p class="lead">Estimated Pick up time: ______</p>
      <p class="lead">Order Placed: _______</p>

      <hr>

      <div class="row" style="display: flex; justify-content: center;" style="color: black">
        <div class="col-md-4 order-md-2 mb-4">
          <h4 class="d-flex justify-content-between align-items-center mb-3; background-color: #fefbd8; ">
          </h4>
          <ul class="list-group mb-3; background-color:burlywood;">

            <li class="list-group-item d-flex justify-content-between lh-condensed;">
              <div>
                <h6 class="my-0" style="color: black"></h6>
                <small class="text-muted"></small>
              </div>

              <span class="text-muted"></span>


              <ul class="list-group mb-3 shadow">
                <%let totalCost = 0%>
                <% for(let i =0;i < orderItem.length;i++){%>
                <li class="list-group-item d-flex justify-content-between lh-condensed">
                  <div>
                    <h6 class="my-0"><%=orderItem[i].name%> x <%=foodArray[i].quantity%></h6>
                    <small class="text-muted"><%=orderItem[i].description%></small>
                  </div>
                  <% let itemPrice = orderItem[i].price * foodArray[i].quantity / 100%>
                  <span class="text-muted">$<%=itemPrice%></span>
                  <%totalCost += itemPrice%>
                  <%}%>

                <li class="list-group-item d-flex justify-content-between">
                  <span>Total:</span>

                  <strong>$<%=totalCost%></strong>
                </li>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </body>
</html>
